import React from "react";
import ReactDOM from "react-dom"
import App from "./App";

ReactDOM.render(
    // can  only return only one element.
    <App />,
    document.getElementById("root")

    // we render the main app here 
)